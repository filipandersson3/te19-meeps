{% extends  layout %}
{% block  content %}
  
<div class="container">
  <h1>{{message}}</h1>
  <div class="card" style="width: 18rem;">
    <div class="card-body">
      <form action="/meeps/" method="POST">
        <label for="body">Meep® something:</label>
        <input type="text" id="body" name="body"><br><br>
        <input type="submit" value="Post">
      </form>
    </div>
  </div>
  <div class="cards"> 
  {% for item in items %}
  <div class="card" style="width: 18rem;">
    <div class="card-body">
      <h5 class="card-title">Anonymous user</h5>
      <p class="card-text">{{item.body}}</p>
      <form action="/videos/{{item.id}}/delete/" method="GET">
        <input type="submit" value="DELETE" class="btn btn-danger">
      </form>
    </div>
  </div>
  {% endfor %}
  </div>
    
</div>
{% endblock %}